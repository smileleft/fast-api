# fast-api test & benchmark
app/
├── main.py          # FastAPI 앱 엔트리 포인트
├── models.py        # SQLAlchemy 모델 정의
├── schemas.py       # Pydantic 모델 (입출력)
├── crud.py          # DB 처리 로직
└── database.py      # DB 연결 및 세션 관리
